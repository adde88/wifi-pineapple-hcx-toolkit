# Aggressive Profile - For active penetration testing.
# Use with: --profile aggressive

# Target everything: APs and clients.
ATTACK_MODE="all"

# Disable power saving for maximum capture performance.
POWER_SAVE_DISABLE=1

# Use a more detailed real-time status display.
RDS_MODE=2

# Automatically use the busiest channels to focus the attack.
# Note: This requires running the script with the --auto-channels flag
# in addition to the profile, or adding it here if you always want it.